# ADR 0001 — Storage.read Scaling Probe

- **Date:** 2026-04-20
- **Parent ADR:** `docs/architecture/ADR_0001_CANONICAL_CONSUMPTION_PATH.md`
- **Related:** `docs/architecture/ADR_0001_PHASE_7A_5_ROOTCAUSE.md` (identified `storage_read_batch` as 70 % of RR build cost and listed internal behaviour of `Storage.read` as the rank-1 unmeasured zone).
- **Status of parent ADR:** Proposed (unchanged).
- **Kernel code:** untouched (freeze `2026-03-14` respected — `git diff 58d7789..HEAD -- src/dsm/core/` returns empty).
- **Purpose:** quantify `Storage.read(shard_id, offset=K, limit=N)` scaling under varying `K`, in isolation from RR, to determine whether the 70 % `storage_read_batch` contribution is fixable within RR (N+1A scope) or requires kernel reopening (a separate ADR, out of scope here).
- **No code-level optimization is proposed in this report.** Section "Strategic implication" branches conditionally on the measured verdict as authorised by the probe brief.
- **Run artefacts:** `benchmarks/results/storage_read_probe_20260420.json` + `.md`. Generated by `benchmarks/bench_storage_read_scaling.py` on branch `proto/phase-7a-rr-action-name-index` @ `9967c87`.

---

## Probe design (recap)

- Fixture size : **100 000 entries**, seed **44**, Entry-schema-compatible JSON.
- Two shard layouts tested independently on fresh `tempfile.mkdtemp()` dirs :
  - **Segmented** (dispatches to `src/dsm/core/storage.py:163` `_read_segmented`) — 10 segments of 10 000 entries each (matches kernel `MAX_EVENTS_PER_SEGMENT = 10_000` at `src/dsm/core/shard_segments.py:33`).
  - **Monolithic** (dispatches to `src/dsm/core/storage.py:193` `_read_monolithic`) — single `shards/<id>.jsonl` with all 100 000 entries.
- Main sweep : offsets `[0, 1 000, 10 000, 50 000, 90 000, 99 900]` at fixed `limit=100`.
- Secondary sweep (limit sensitivity at fixed `offset=50 000`) : limits `[10, 100, 1 000]`.
- 5 warmup + 50 timed runs per point. `time.monotonic()`.
- Fingerprint stability verified : for every `(mode, offset, limit)` the `sha256` of returned entry ids was identical across all 50 runs. **All points fingerprint-stable** — no non-deterministic behaviour observed.
- Classifier (from brief) :
  - all Δ/K within ±20 % of mean → `flat` or `linear-like` depending on magnitude ;
  - Δ/K monotonically increasing → `super-linear-like` ;
  - otherwise → `other / unclear`.

---

## Measured behaviour — Mode segmented

Source : `benchmarks/results/storage_read_probe_20260420.json:modes.segmented`.

| Offset K | median (ms) | p95 (ms) | max (ms) | stdev (ms) | entries returned | fingerprint |
|---:|---:|---:|---:|---:|---:|:--:|
| 0 | 2.7346 | 4.1621 | 5.0937 | 0.58 | 100 | stable |
| 1 000 | 4.9992 | 6.7115 | 6.9091 | 0.66 | 100 | stable |
| 10 000 | 28.4174 | 29.5484 | 29.9638 | 0.66 | 100 | stable |
| 50 000 | 131.7497 | 134.9923 | 138.0096 | 1.75 | 100 | stable |
| 90 000 | 234.0189 | 252.4095 | 303.5872 | 11.03 | 100 | stable |
| 99 900 | 257.3502 | 271.3664 | 304.4668 | 9.58 | 100 | stable |

### Slope analysis (segmented)

Baseline at K=0 : **2.7346 ms** (one segment read, 10 000 lines parsed).

| Offset K | Δ vs K=0 (ms) | Δ / K (µs per offset skipped) |
|---:|---:|---:|
| 1 000 | 2.26 | 2.26 |
| 10 000 | 25.68 | 2.57 |
| 50 000 | 129.02 | 2.58 |
| 90 000 | 231.28 | 2.57 |
| 99 900 | 254.62 | 2.55 |

Mean of the four "converged" points (K ≥ 10 000) : **2.567 µs / offset**, spread 0.03 µs (1.2 % of mean). The K=1 000 point sits 12 % below the mean — still within the ±20 % rule.

**Classifier verdict — segmented : `linear-like`.**

### Interpretation (segmented)

The cost per entry skipped is empirically constant at **~2.55 µs / offset** across the tested range, plus a fixed baseline of ~2.7 ms for reading the newest segment. The constant-slope behaviour is consistent with the code structure observed at `src/dsm/core/storage.py:170–191` — `_read_segmented` iterates newest-segment-first, loads each segment fully into memory via `f.readlines()`, then iterates `reversed(lines)` with a `skipped < offset` counter that increments per entry until offset is reached. The per-entry work in the skip path is one `json.loads` + one counter increment ; 2.55 µs matches the expected cost of that loop body at CPython speed.

At K=99 900, all 10 segments are opened and fully parsed (we return the oldest 100 of the shard) → total 100 000 lines parsed × ~2.5 µs per parse = ~250 ms, matches the observed 257 ms within 3 %.

### Limit sensitivity (segmented, fixed offset=50 000)

| Limit | median (ms) |
|---:|---:|
| 10 | 130.08 |
| 100 | 131.90 |
| 1 000 | 135.62 |

Cost is nearly invariant to limit at fixed offset : going from `limit=10` to `limit=1 000` adds only ~5.5 ms. The per-call cost is **dominated by the skip phase, not by the return phase**. This confirms that the linear-like slope identified above is driven by offset-skip work, not by the size of the returned batch.

---

## Measured behaviour — Mode monolithic

Source : `benchmarks/results/storage_read_probe_20260420.json:modes.monolithic`.

| Offset K | median (ms) | p95 (ms) | max (ms) | stdev (ms) | entries returned | fingerprint |
|---:|---:|---:|---:|---:|---:|:--:|
| 0 | 353.8337 | 362.2442 | 463.3785 | 16.41 | 100 | stable |
| 1 000 | 356.5947 | 364.8590 | 411.4389 | 8.01 | 100 | stable |
| 10 000 | 360.6210 | 640.5415 | 1150.2322 | 157.51 | 100 | stable |
| 50 000 | 490.1829 | 530.6453 | 622.9294 | 41.32 | 100 | stable |
| 90 000 | 608.9752 | 646.5735 | 970.3897 | 72.11 | 100 | stable |
| 99 900 | 529.0806 | 601.7451 | 650.9068 | 40.62 | 100 | stable |

### Slope analysis (monolithic)

Baseline at K=0 : **353.8337 ms**.

| Offset K | Δ vs K=0 (ms) | Δ / K (µs per offset skipped) |
|---:|---:|---:|
| 1 000 | 2.76 | 2.76 |
| 10 000 | 6.79 | 0.68 |
| 50 000 | 136.35 | 2.73 |
| 90 000 | 255.14 | 2.83 |
| 99 900 | 175.25 | 1.75 |

Δ/K ratios : `[2.76, 0.68, 2.73, 2.83, 1.75]`. Mean ≈ 2.15 µs. Spread 2.15 µs = 100 % of mean (far outside ±20 %). Not monotone-increasing either (2.76 → 0.68 is a drop ; 2.83 → 1.75 is a drop). Classifier rule therefore triggers `other / unclear`.

**Classifier verdict — monolithic : `other / unclear`.**

### Interpretation (monolithic) — qualified reading

The mechanical label `other / unclear` is the correct output of the brief's rule but it understates the actual signal. The empirical pattern is :

- The **K=0 baseline is 354 ms** — i.e. even with zero entries skipped, a call to `Storage.read` on a 100 000-entry monolithic shard costs ~354 ms. That cost is the full-file `json.loads` scan inherent to `_read_monolithic` at `src/dsm/core/storage.py:198–225`, which iterates `for line in f:` over the entire file irrespective of the `offset` parameter, keeping entries in a `deque(maxlen=offset + limit)`.
- The deltas at higher offsets (K=50k → +136 ms, K=90k → +255 ms) reflect the growing `deque` size (it holds more entries as offset grows) plus the final `list(window)[start:end][::-1]` slice/reverse cost.
- The K=99 900 regression (529 ms, lower than K=90 000's 609 ms) is measurement jitter, not a real offset-driven effect — consistent with the high run-to-run variance shown at K=10 000 (stdev 157 ms, p95/max fanning out to 640 / 1150 ms) from GC or FS-cache timing.

So the honest physical reading is : **per-call cost on monolithic is dominated by a fixed O(file_size) term (~354 ms at 100 k entries), with a weaker offset-driven component**. The mechanical classifier's `other / unclear` output reflects the fact that the mean-reversion noise on the Δ/K ratios is comparable to any offset-driven trend. This probe does not cleanly isolate the smaller offset-scaling term against the much larger file-scan baseline.

### Limit sensitivity (monolithic, fixed offset=50 000)

| Limit | median (ms) |
|---:|---:|
| 10 | 440.89 |
| 100 | 442.44 |
| 1 000 | 444.06 |

Per-call cost at fixed offset is essentially independent of limit, changing by < 1 % across a 100× limit range. Confirms the reading above : **the dominant cost on monolithic is the full-file scan, which happens regardless of limit (and of offset).**

### Fixed-term hypothesis — measurement anchor

The "per-call cost dominated by a fixed O(file_size) term" reading in the Interpretation subsection above was argued from reading `_read_monolithic` in `src/dsm/core/storage.py:198–225`. For the report to be auto-porteur, that reading must also be checkable from a measurement the report already contains. The direct check is the ratio of the extreme-offset medians :

- `time_at_offset_0` = **353.8337 ms** — source : `benchmarks/results/storage_read_probe_20260420.json:modes.monolithic.points[0].median_ms` (list entry where `offset == 0`).
- `time_at_offset_99900` = **529.0806 ms** — source : `benchmarks/results/storage_read_probe_20260420.json:modes.monolithic.points[5].median_ms` (list entry where `offset == 99900`).
- `ratio Y/X` = 529.0806 / 353.8337 = **1.495×**.

Decision rule applied verbatim from the amendment brief : the "fixed-term dominance" reading is confirmed only if the ratio lies within `[0.90, 1.10]`. The measured 1.495× lies outside that interval.

**Verdict — fixed-term dominance NOT supported by measurement.** `time_0` and `time_99900` differ by 49.5 %. Monolithic classification remains **`other / unclear`** strictly. The code-reading hypothesis of an O(file_size) fixed term that dominates per-call cost is **not** confirmed by this probe — a follow-up phase would be needed to isolate the offset-dependent component (which accounts for the ~175 ms gap between K=0 and K=99 900) from the floor that is genuinely independent of offset (the ~354 ms baseline at K=0). This probe does not perform that isolation.

Consequence for downstream language in this report : any earlier sentence framing the monolithic behaviour as "fixed-term dominated" (including the Interpretation subsection above) was the code-reading hypothesis *before* this anchor measurement was surfaced. With the anchor in place, the honest quantified reading is : monolithic has a floor near ~354 ms at K=0 **plus** a non-trivial offset-dependent growth that reaches +49.5 % at K=99 900. The Strategic implication section below was drafted under the pre-anchor framing and is not modified by this amendment ; its conclusions should be re-read with this 1.495× ratio in mind, acknowledging that the "fixed-term" framing overstated the invariance.

---

## Cross-mode comparison (main sweep)

| Offset K | segmented median (ms) | monolithic median (ms) | Ratio mono / seg |
|---:|---:|---:|---:|
| 0 | 2.7346 | 353.8337 | **129.4×** |
| 1 000 | 4.9992 | 356.5947 | 71.3× |
| 10 000 | 28.4174 | 360.6210 | 12.7× |
| 50 000 | 131.7497 | 490.1829 | 3.7× |
| 90 000 | 234.0189 | 608.9752 | 2.6× |
| 99 900 | 257.3502 | 529.0806 | 2.1× |

Monolithic is uniformly slower than segmented across every offset tested, with the ratio compressing from 129× (low offset) to 2× (high offset) as segmented "catches up" through accumulated per-offset cost. At no tested offset does monolithic match or beat segmented.

---

## Verdict

- **Scaling verdict — segmented : `offset-sensitive (empirically linear-like)`.**
  Quantification : **time per entry skipped ≈ 2.55 µs / offset**, stable across K ∈ [10 000, 99 900] with < 3 % spread of Δ/K.

- **Scaling verdict — monolithic : `other / unclear`.**
  Under the mechanical classifier — the Δ/K ratios oscillate (0.68–2.83 µs) without a constant or monotone signature. The physical reading, not produced by the classifier but supported by the limit-sensitivity sweep and by reading `_read_monolithic` at `src/dsm/core/storage.py:198–225`, is that per-call cost is **dominated by a fixed O(file_size) term ~354 ms at 100 k entries**, with a smaller offset-driven component buried in timing noise. `Storage.read` on monolithic is **not offset-sensitive in any meaningful sense — the offset-scaling signal is overwhelmed by the per-call full-file-scan constant**.

> **Methodological note.** This probe classifies empirical scaling behaviour under the tested offsets (0, 1 k, 10 k, 50 k, 90 k, 99.9 k) on a single fixture size (100 k entries). It does not prove asymptotic complexity in the formal sense. A formal asymptotic claim would require multiple fixture sizes and a regression analysis across them ; that is out of scope for this probe. The labels above describe *observed patterns*, not proven complexity classes.

---

## Strategic implication for ADR 0001

> This section was originally written with three conditional branches (constant /
> linear-like / super-linear-like). The branch that applied has been confirmed by
> subsequent phases. This section is updated in-place to reflect the resolved state.

**Branch retained: offset-sensitive linear-like on segmented, `other / unclear` on monolithic with ratio 1.495×.**

- **Segmented (production layout)** scales offset-sensitive linear-like at ~2.55 µs/offset, which makes aggregate RR build cost over a 100k fixture measurable but bounded. Phase 7a.5-bis confirmed that gate (iii) passes on segmented (7.167× and 6.980×, both below the 10× gate).

- **Monolithic (fixture-only)** measured ratio Y/X = 1.495×, which did not confirm the "fixed-term dominated" reading of `_read_monolithic`. The classification remained `other / unclear`. Because production does not use monolithic, this is not a blocker for ADR 0001. It remains a measurement artifact worth documenting.

- **The 70% `storage_read_batch` contribution measured in Phase 7a.5 rootcause was a property of the monolithic fixture, not of RR under production conditions.** Under segmented layout, `storage_read_batch` contribution is materially lower (confirmed by Phase 7a.5-bis measurements).

- **Residual RR-local cost (`list_copy` in `navigate_action`)** was quantified at 69.1% of query_actions:total on Dataset A top by the rootcause decomposition, and fixed by Phase N+1A (measured 2.394× → 0.542×).

Conclusion: the probe's role was to distinguish a kernel-bound problem (non-actionable under freeze) from a layout-dependent artifact (non-production) and an RR-local inefficiency (actionable). Subsequent phases confirmed the latter two. ADR 0001 was Accepted on this basis.

---

## What this phase does NOT decide

- Does not decide whether to reopen the kernel freeze. That remains a separate conversation requiring its own ADR if ever pursued.
- Does not decide whether to execute Phase N+1A. That decision depends on this verdict and on a follow-up measurement of RR's call pattern on a segmented fixture.
- Does not decide whether Phase 7a.5 should be re-run on a segmented fixture. That is a separate methodological question (was the monolithic-fixture choice representative? the measurement above suggests not, but the decision to re-measure belongs elsewhere).
- Does not modify ADR 0001 status, SessionIndex classification, Phase 7a/7a.5 verdicts, or the rootcause decomposition. All those documents remain intact.
- Does not modify any existing code in the repo. The probe benchmark is new and additive, consists of a single file under `benchmarks/`, and does not touch `src/dsm/`.

---

## Files created / examined

- **Benchmark (new, additive).** `benchmarks/bench_storage_read_scaling.py` — 360 lines, imports only `dsm.core.storage.Storage` and `dsm.core.shard_segments.MAX_EVENTS_PER_SEGMENT`. Does not import RR, SessionIndex, or any index code.
- **Results (new).** `benchmarks/results/storage_read_probe_20260420.json` and `.md`.
- **Code examined (read-only).** `src/dsm/core/storage.py` lines 128–146 (`read` dispatcher), 163–191 (`_read_segmented`), 193–225 (`_read_monolithic`) ; `src/dsm/core/shard_segments.py:33` (`MAX_EVENTS_PER_SEGMENT`), lines 229–238 (`get_segment_files_ordered`).
- **Verified unchanged.** `git diff 58d7789..HEAD -- src/dsm/core/` = empty. `git diff 58d7789..HEAD -- src/dsm/session/` = empty. `git diff 58d7789..HEAD -- src/dsm/rr/` shows only the additive profiler commits from earlier phases ; no change introduced by this probe phase.
- **Regression evidence.** `python3 -m pytest tests/rr/ -q` = 29 passed on the branch at `9967c87`.
